# Template de Tarefa – Board do Projeto

> Este arquivo serve como **esqueleto obrigatório** para qualquer tarefa dentro de `docs/board`.
> O agente **não pode** criar tarefas fora deste formato.

---

## Identificação
- **ID da tarefa:** API-009
- **Título curto:** Meta: último mês acessado
- **Status:** TO-DO
- **Responsável (agente):** GPT-5.1-Codex-Max
- **Reasoning effort:** High

---

## Contexto
O sistema deve lembrar o último mês acessado. A API precisa persistir essa informação em `meta.json` com o formato `YYYY-MM`.

---

## Escopo da Tarefa
### Inclui
- Ler e atualizar `meta.json` com `lastOpenedMonth`.
- Validar formato `YYYY-MM`.

### Não inclui (importante)
- Qualquer lógica de navegação no cliente.
- Armazenamento de preferências não descritas no modelo.

---

## Regras de Negócio Relacionadas
- ideias.md: 10.2
- regras-de-negocio.md: 10.2
- modelos-json.md: 9

---

## Arquivos Impactados
- `server/...`

---

## Passos de Implementação Esperados
1. Implementar persistência de `meta.json` usando o utilitário de JSON.
2. Implementar validação de formato `YYYY-MM`.
3. Expor operações de leitura e atualização do valor.

> O agente pode ajustar os passos, mas **não pode sair do escopo**.

---

## Critérios de Aceite
A tarefa só pode ser movida para **DONE** se **todos** forem verdadeiros:
- [ ] Regra de negócio respeitada
- [ ] Persistência segue o modelo JSON
- [ ] Não houve criação de funcionalidade fora do escopo
- [ ] Fluxo mínimo funcional validado manualmente

---

## Como Testar (Manual)
1. Definir `lastOpenedMonth` como um valor válido (`YYYY-MM`).
2. Ler `meta.json` e verificar que o valor persiste corretamente.
3. Tentar salvar um valor inválido e confirmar que é rejeitado.

---

## Pendências / Observações
- Nenhuma.